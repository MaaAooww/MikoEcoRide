RewriteEngine On

# Si fichier/dossier existe, on le sert directement
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon, on redirige vers index.php
RewriteRule ^ index.php [L]
